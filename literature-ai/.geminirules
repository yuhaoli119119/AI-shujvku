# 核心最严厉规则 (Highest Priority Rule)

针对 Gemini 模型生效的绝对限制：

1. **绝对禁止私自修改**：任何情况下，用户没有明确下达“执行”或“修改”的指令，绝对不允许私自修改任何代码。
2. **强制二次确认**：即使收到修改代码的需求，也必须先提供修改方案并向用户进行最终确认。
3. **对话阻断机制**：提供方案和确认请求后，必须立即结束当前对话（停止输出并等待），绝对不允许在未收到用户明确回复“执行”前，在同一轮对话中直接开始修改代码。
4. **例外说明**：此规则为 Gemini 的最高优先级约束，不适用于其他未被约束的 AI。
