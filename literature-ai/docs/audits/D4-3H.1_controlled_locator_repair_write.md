# D4-3H.1 Controlled Locator Repair Write Gate / 3-item Narrow Active Write

Date: 2026-05-28

Scope: first controlled active locator repair write gate. This round wrote only locator rows for three explicitly approved pending review rows. It did not mark any review verified, did not unlock export/writing, and did not write `name / Fe-Co-V` or `convergence_settings`.

## 1. Baseline / Sync

Required preflight commands:

- `git status --short`: clean
- `git log -1 --oneline`: `9add944 docs d4 locator repair human confirmation plan`
- `git rev-parse HEAD`: `9add944c79f92225fcb29ee63569f7b4b52fe2e4`
- `git branch -vv`: `* master 9add944 [origin/master] docs d4 locator repair human confirmation plan`
- `git fetch origin`: succeeded
- `git ls-remote origin refs/heads/master`: `9add944c79f92225fcb29ee63569f7b4b52fe2e4 refs/heads/master`

Conclusion: D4-3H.1 started from the requested baseline, with local `HEAD`, local `origin/master`, and remote `refs/heads/master` aligned.

## 2. Active DB Pre-write Safety Check

Canonical active DB:

- `D:\Desktop\03_代码与开发\AI-shujvku\literature-ai\data\libraries\default\database.sqlite`

Pre-write read-only check:

- `papers_total`: 15
- pilot paper exists: yes
- pilot title: `锂硫电池非均相电催化剂`
- pilot review rows: 5
- pending rows: 5
- verified rows: 0
- safe verified rows: 0
- pilot evidence locators: 0
- export eligible: 0
- writing eligible: 0

The initial direct absolute-path check hit a Windows console encoding issue and opened a wrong mojibake path with no tables. The actual safety check and write used the repository-relative canonical DB path resolved to the correct 15-paper DB above.

## 3. Approved Write Scope

Product-approved write items:

| Review ID | Field | Value | Page | Bbox | Source artifact | Match method | Confidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `09f83676-8f13-4e82-a576-ab359b264933` | `catalyst_type` | `single_atom` | 7 | `{x0:53.858,y0:477.052,x1:287.155,y1:359.672,coordinate_system:BOTTOMLEFT}` | Docling `#/texts/79` | `substring_match` | 0.68 |
| `280f2d9e-3ebb-4107-9702-f6ea6d645465` | `metal_centers` | `["Fe","Co","V"]` | 7 | `{x0:53.859,y0:356.594,x1:287.167,y1:70.08500000000004,coordinate_system:BOTTOMLEFT}` | Docling `#/texts/80` | `substring_match` | 0.68 |
| `56f72584-45b3-465b-9a40-97ec60a2fabf` | `rate` | `0.2C` | 6 | `{x0:53.858,y0:125.995,x1:541.43,y1:71.08699999999999,coordinate_system:BOTTOMLEFT}` | Docling `#/texts/74` | `substring_match` | 0.68 |

Explicitly not approved:

- `e2c75b7f-2d9c-41ff-a6e1-e95e5d491896` / `name` / `Fe-Co-V`: not written; remains HIGH-CAUTION because it is a normalized aggregate, not a literal source phrase.
- `4ba0e490-5934-439c-8136-33a8ddf4e201` / `convergence_settings`: not written; remains RED excluded because current evidence is an extracted empty-settings dict with no reliable source artifact.

## 4. Implementation

New backend service:

- `backend/app/services/locator_repair_write_service.py`

Safety behavior:

- default `dry_run=True`
- active write requires `allow_active_db_write=True`
- active write requires `confirmed_approval=True`
- reads D4-3H.0 disabled plan and D4-3G manifest, then hydrates approved items with matched text/source artifact
- only allows fields `catalyst_type`, `metal_centers`, and `rate`
- rejects `name`, `convergence_settings`, unknown fields, non-pilot paper, missing review IDs, missing page, missing bbox, confidence below threshold, and verified/export/writing-like payloads
- writes through existing `EvidenceLocatorService.upsert_locator()`
- does not write or mutate `ExtractionFieldReview`
- does not import or call `mark_verified`, `save_reviews`, migration, extraction apply, materialize, or registry write paths

## 5. Rollback Snapshot

Rollback snapshot path:

- `docs/audits/D4-3H.1_locator_repair_rollback_snapshot.json`

Snapshot contents:

- active DB path
- timestamp
- target review IDs
- full pre-write pilot review row state
- pre-write target locator state
- pre-write verified/safe-verified/export/writing status
- non-pilot review checksum and non-pilot locator count

No DB copy was created. The rollback artifact is a JSON state snapshot only.

## 6. Active Write Result

Write command used the service with:

- `allow_active_db_write=True`
- `confirmed_approval=True`
- `dry_run=False`
- approved review IDs exactly:
  - `09f83676-8f13-4e82-a576-ab359b264933`
  - `280f2d9e-3ebb-4107-9702-f6ea6d645465`
  - `56f72584-45b3-465b-9a40-97ec60a2fabf`

Rows written:

- 3 `evidence_locators` rows
- locator status: `exact_page`
- parser source: `docling`
- source type: `text`
- warning reason includes `controlled_locator_repair:D4-3H.1`, review_id, source_artifact, match_method, and `locator_only_no_review_change`

No review row was updated.

## 7. Post-write Verification

Post-write read-only check:

- `papers_total`: 15
- pilot review rows: 5
- pending rows: 5
- verified rows: 0
- safe verified rows: 0
- pilot evidence locators: 3
- `name` locators: 0
- `convergence_settings` locators: 0
- export eligible: 0
- writing eligible: 0

Written locators:

| Field | Target type | Target ID | Page | Locator status | Confidence | Parser |
| --- | --- | --- | --- | --- | --- | --- |
| `catalyst_type` | `catalyst_samples` | `d4811c89-f92e-4fe1-ae05-5350fe5d4c71` | 7 | `exact_page` | 0.68 | `docling` |
| `metal_centers` | `catalyst_samples` | `d4811c89-f92e-4fe1-ae05-5350fe5d4c71` | 7 | `exact_page` | 0.68 | `docling` |
| `rate` | `electrochemical_performance` | `b1598378-effe-4316-9857-ae4bedc515c9` | 6 | `exact_page` | 0.68 | `docling` |

## 8. Test Coverage

New test file:

- `backend/tests/test_d4_locator_repair_write_service.py`

Coverage:

1. default dry-run does not write DB
2. missing explicit approval rejects active write
3. only approved three fields are written
4. `name / Fe-Co-V` is rejected
5. `convergence_settings` is rejected
6. non-pilot paper is rejected
7. locator write does not set verified
8. locator write does not set `reviewer_status=verified`
9. locator write does not unlock export
10. locator write does not unlock writing
11. rollback snapshot is generated before write
12. pending review status remains pending
13. missing page is rejected without fabrication
14. missing bbox is rejected without fabrication
15. integration only touches approved review IDs / target locator rows
16. verified-like payload is rejected

Focused test result:

- `py -m pytest tests/test_d4_locator_repair_write_service.py`
- result: `10 passed, 164 warnings`

Full backend verification:

- `py -m compileall app findpapers tests`: passed, exit code 0
- `py -m pytest`: `368 passed, 790 warnings in 255.17s`
- `git diff --check`: passed, exit code 0

Frontend Playwright was not run because no frontend files were modified.

## 9. Safety Confirmation

- active DB write: yes, locator rows only, exactly 3 approved rows
- locator write: yes, exactly 3 approved locator rows
- `name / Fe-Co-V`: not written
- `convergence_settings`: not written
- verified review write: no
- `mark_verified`: no
- `save_reviews`: no
- `reviewer_status=verified`: no
- `verified=true`: no
- `safe_verified=true`: no
- export/writing unlock: no
- migration apply: no
- extraction/reprocessing apply: no
- materialize: no
- artifact cleanup: no
- DB copy/move/delete: no
- registry write: no
- canonical registry modification: no

## 10. Remaining Risks

- Locator repair still does not mean human verification.
- Review rows remain pending and must not be treated as safe evidence.
- Bboxes should be checked in the human workbench/PDF view before any later verification gate.
- D4-3H.2 / D4-3I should handle UX and human verification separately; D4-3H.1 must not be followed by automatic `mark_verified`.
